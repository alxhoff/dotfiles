# Paths to restore from OLD_HOME -> NEW_HOME via rsync.
# One path per line (relative to home). Lines starting with # are ignored.
# Managed by migrate/restore-from-backup.sh — edit to add/remove targets.
#
# Priority: git/ is ~770GB on this machine — ensure enough disk space.

# --- Critical: projects ---
git

# --- Secrets (never commit these to the dotfiles repo) ---
.ssh
.gnupg

# --- Cursor / VS Code state ---
.config/Cursor
.cursor
.config/Code

# --- Browser ---
.mozilla/firefox

# --- Docker Windows VM (large; optional if not using windows fish function) ---
windows
window_data
compose.yaml

# --- Spotify login state (not the 10GB cache) ---
.config/spotify

# --- Cloud / work CLI state ---
.config/gcloud
.config/fish/fish_variables
.local/share/fish

# --- Optional home-level project dirs (uncomment if used) ---
# Cartken

# --- Caches: excluded by default (huge, reproducible) ---
# .cache
