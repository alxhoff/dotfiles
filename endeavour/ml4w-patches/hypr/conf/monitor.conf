# Layout lives in monitors.conf (updated by endeavour/displays on dock hotplug).
# validate-monitors-conf.sh runs before Hyprland starts (dotfiles-start-hyprland wrapper).
source = ~/.config/hypr/monitors.conf
