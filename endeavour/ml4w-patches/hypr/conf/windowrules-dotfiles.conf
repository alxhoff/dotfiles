# Dotfiles window rules (Hyprland 0.55+)

# Remote viewers: tiled resize can break the viewport; keep floating on open.
windowrule {
    name = vncviewer-float
    match:class = ^realvnc-vncviewer$
    float = on
    center = on
}
