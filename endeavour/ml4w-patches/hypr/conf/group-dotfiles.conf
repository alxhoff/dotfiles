# Hyprland window groups (i3 tabbed / stacking)

group {
    auto_group = off
    focus_removed_window = true

    groupbar {
        enabled = true
        font_size = 11
        height = 18
        stacked = false
    }
}
