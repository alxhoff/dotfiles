# Dotfiles: do not sleep when the lid closes on external power (home/work dock, AC).
# Matches hypridle + power-menu policy (suspend/hibernate only on battery).
# USB-C docks often never set logind "Docked", so HandleLidSwitchDocked=ignore alone is not enough.
[Login]
HandleLidSwitchExternalPower=ignore
