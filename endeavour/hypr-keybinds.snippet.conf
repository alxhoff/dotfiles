# Dotfiles — merge into ~/.config/hypr/conf/binds.conf if desired
# Example: terminal (kitty is ML4W default — adjust to your terminal)

# bind = $mainMod, RETURN, exec, kitty
# bind = $mainMod, D, exec, rofi -show drun
